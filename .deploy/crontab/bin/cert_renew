#!/usr/bin/env bash
set -e

cd /home/ubuntu/webapp/gemsla.be/.deploy

/usr/bin/docker compose -f docker-compose.utils.yml run --rm --no-deps certbot-renew

/usr/bin/docker compose -f docker-compose.yml exec -T nginx nginx -s reload